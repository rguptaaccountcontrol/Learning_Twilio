{
  "name": "twilio-ci-test",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "twilio-run --env"
  },
  "devDependencies": {
    "twilio-run": "^2.0.0"
    "ccnpm": "3.0.2"
  },
  "engines": {
    "node" : ">=8.0.0 <11.0.0" 
  }
}
